{
  "name": "hatch",
  "version": "1.0.2",
  "description": "Universal Cmd+K command palette for the browser",
  "private": true,
  "author": {
    "name": "Sounak Das"
  },
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist",
    "package": "npm run clean && npm test && npm run build && cd dist && zip -rX \"../hatch-v$npm_package_version.zip\" . -x '.*' -x '*/.*'"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "quickfuzz": "^1.0.2"
  }
}
